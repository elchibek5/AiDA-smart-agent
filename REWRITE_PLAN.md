# Rewrite: AlgoMentor (Java + Web)

This repo started as an early prototype. Current work is a rewrite on branch:
rewrite/algomentor-java

Goal: ship AlgoMentor v1.0 (React + Spring Boot) with:
- /api/health
- /api/analyze (LLM JSON report)
- deployed demo + README examples
